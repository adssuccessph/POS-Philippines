(()=>{
'use strict';
const API=(window.POS_CONFIG?.API_BASE||'').replace(/\/$/,'');
const TOKEN_KEY='pos_ph_admin_token';
const STATUSES=['New Inquiry','Reviewing Requirements','Quotation Prepared','Waiting for Customer','Approved / Scheduled','Completed','Cancelled'];
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=(v='')=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const peso=n=>Number(n)>0?new Intl.NumberFormat('en-PH',{style:'currency',currency:'PHP',maximumFractionDigits:0}).format(Number(n)):'Ask for Quote';
const slug=v=>String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
const fmt=v=>v?new Date(v).toLocaleString('en-PH',{year:'numeric',month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}):'—';
const plural=type=>type==='category'?'categories':`${type}s`;
const ADMIN_FAST_REFRESH_MS=15000;
const ADMIN_FULL_REFRESH_MS=60000;
let fastRefreshTimer=null;
let fullRefreshTimer=null;
let refreshBusy=false;
let token=localStorage.getItem(TOKEN_KEY)||'', activeView='dashboard', store={settings:{},categories:[],brands:[],products:[],packages:[],inquiries:[],reviews:[],dashboard:{}};
function notify(msg,type=''){const t=$('#toast');t.textContent=msg;t.className='toast show '+type;clearTimeout(notify.timer);notify.timer=setTimeout(()=>t.className='toast',3000)}
async function api(path,options={},auth=true){if(!API||API.includes('REPLACE-WITH'))throw new Error('API is not configured. Add your Cloudflare Worker URL in config.js.');const headers={...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),...(options.headers||{})};if(auth&&token)headers.Authorization=`Bearer ${token}`;const res=await fetch(API+path,{...options,headers});let body={};try{body=await res.json()}catch{}if(res.status===401&&auth){logout(false);throw new Error('Session expired. Please log in again.')}if(!res.ok)throw new Error(body.error||`Request failed (${res.status})`);return body}
function openDialog(id){const d=$('#'+id);if(d&&!d.open)d.showModal()}
function closeDialog(id){const d=$('#'+id);if(d?.open)d.close()}
function imageCell(url,name){return url?`<img src="${esc(url)}" alt="${esc(name)}">`:`<div style="width:48px;height:44px;border-radius:8px;background:#edf4ff;display:grid;place-items:center;color:#1764eb;font-weight:900">${esc(name).slice(0,2).toUpperCase()}</div>`}
function loginView(error=''){$('#app').innerHTML=`<main class="admin-login"><section class="login-info"><div class="brand-fallback" style="width:78px;height:78px;font-size:20px;background:#fff;color:#071c48">POS</div><h1>Website <span>Admin Dashboard</span></h1><p>Manage website branding, products, packages, inquiries, customer tracking, and reviews using the live Cloudflare database.</p><div class="login-points"><article><b>Live Database</b><small>Changes are saved online and visible across devices.</small></article><article><b>Secure Login</b><small>Password hashing and expiring database sessions.</small></article><article><b>Image Storage</b><small>Product and package images are stored in Cloudflare R2.</small></article><article><b>Mobile Ready</b><small>Use the admin dashboard on phone, tablet, or desktop.</small></article></div></section><section class="login-box-wrap"><form id="loginForm" class="login-card panel"><h2>Admin Login</h2><p>Enter your administrator account.</p>${API.includes('REPLACE-WITH')?'<div class="api-warning">Cloudflare API is not configured yet. Edit <b>config.js</b> after deploying the Worker.</div>':''}<label>Username</label><input name="username" class="control" value="admin" required><label style="margin-top:12px">Password</label><input name="password" type="password" class="control" required><button class="btn btn-blue btn-block" style="margin-top:13px">Log In</button><a href="./index.html" class="btn btn-outline btn-block" style="margin-top:8px">Return to Website</a><div id="loginError" class="small" style="color:#b52c2c;margin-top:9px">${esc(error)}</div></form></section></main>`;$('#loginForm').onsubmit=login}
async function login(e){e.preventDefault();const f=e.currentTarget;try{const result=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:f.username.value.trim(),password:f.password.value})},false);token=result.token;localStorage.setItem(TOKEN_KEY,token);if(result.must_change_password)passwordSetupView();else await startAdmin()}catch(err){$('#loginError').textContent=err.message}}
function passwordSetupView(){$('#app').innerHTML=`<main class="admin-login"><section class="login-info"><div class="brand-fallback" style="width:78px;height:78px;font-size:20px;background:#fff;color:#071c48">POS</div><h1>Create Your <span>New Password</span></h1><p>The temporary password must be replaced before the dashboard can be used.</p></section><section class="login-box-wrap"><form id="setupPasswordForm" class="login-card panel"><h2>Set New Password</h2><p>Use at least 8 characters.</p><label>Current Temporary Password</label><input name="current_password" type="password" class="control" required><label style="margin-top:12px">New Password</label><input name="new_password" type="password" minlength="8" class="control" required><label style="margin-top:12px">Confirm Password</label><input name="confirm_password" type="password" minlength="8" class="control" required><button class="btn btn-blue btn-block" style="margin-top:13px">Save New Password</button><div id="passwordError" class="small" style="color:#b52c2c;margin-top:9px"></div></form></section></main>`;$('#setupPasswordForm').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget;if(f.new_password.value!==f.confirm_password.value){$('#passwordError').textContent='New passwords do not match.';return}try{await api('/api/auth/change-password',{method:'POST',body:JSON.stringify({current_password:f.current_password.value,new_password:f.new_password.value})});notify('Password changed. Please log in again.','success');logout()}catch(err){$('#passwordError').textContent=err.message}}}
function stopAutoRefresh(){clearInterval(fastRefreshTimer);clearInterval(fullRefreshTimer);fastRefreshTimer=null;fullRefreshTimer=null}
function logout(callApi=true){stopAutoRefresh();if(callApi&&token)api('/api/auth/logout',{method:'POST'}).catch(()=>{});token='';localStorage.removeItem(TOKEN_KEY);loginView()}
async function startAdmin(){try{const me=await api('/api/auth/me');if(me.must_change_password){passwordSetupView();return}renderShell();await loadAll();startAutoRefresh()}catch(err){loginView(err.message)}}
function renderShell(){$('#app').innerHTML=`<div class="admin-shell"><aside class="sidebar"><div class="side-brand"><div class="fallback">POS</div><div><strong id="sideName">POS Philippines</strong><small>Live Website Admin</small></div></div><nav class="side-nav"><button class="active" data-view="dashboard">▦ Dashboard</button><button data-view="settings">⚙ Website Settings</button><button data-view="categories">▣ Categories</button><button data-view="brands">◆ Brands</button><button data-view="products">▤ Products</button><button data-view="packages">▥ Packages</button><button data-view="inquiries">✉ Inquiries</button><button data-view="reviews">★ Reviews</button><button data-view="password">🔑 Change Password</button><button class="danger" id="logout">↪ Log Out</button></nav></aside><div class="admin-main"><header class="topbar"><h1 id="pageTitle">Dashboard</h1><div class="top-actions"><span id="autoRefreshState" class="small muted">Auto-refresh: ON</span><a href="./index.html" target="_blank">View Website ↗</a><button id="refreshAll" class="btn btn-outline btn-sm">Refresh</button></div></header><main class="content"><div id="apiState"></div><section id="view-dashboard" class="section-view active"></section><section id="view-settings" class="section-view"></section><section id="view-categories" class="section-view"></section><section id="view-brands" class="section-view"></section><section id="view-products" class="section-view"></section><section id="view-packages" class="section-view"></section><section id="view-inquiries" class="section-view"></section><section id="view-reviews" class="section-view"></section><section id="view-password" class="section-view"></section></main></div></div>`;$$('.side-nav [data-view]').forEach(b=>b.onclick=()=>openView(b.dataset.view));$('#logout').onclick=()=>logout();$('#refreshAll').onclick=()=>loadAll(false);$$('[data-close]').forEach(b=>b.onclick=()=>closeDialog(b.dataset.close));document.querySelectorAll('dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}))}
async function loadAll(showNotice=true){if(refreshBusy)return;refreshBusy=true;try{const [dashboard,settings,categories,brands,products,packages,inquiries,reviews]=await Promise.all([api('/api/admin/dashboard'),api('/api/admin/settings'),api('/api/admin/categories'),api('/api/admin/brands'),api('/api/admin/products'),api('/api/admin/packages'),api('/api/admin/inquiries'),api('/api/admin/reviews')]);store={dashboard,settings,categories,brands,products,packages,inquiries,reviews};$('#apiState').innerHTML='';$('#sideName').textContent=settings.business_name||'POS Philippines';renderAll();if(showNotice)notify('Live data refreshed.','success')}catch(err){$('#apiState').innerHTML=`<div class="api-warning">${esc(err.message)}</div>`;if(showNotice)notify(err.message,'error')}finally{refreshBusy=false}}
function canAutoRefresh(){if(!token||document.visibilityState!=='visible'||document.querySelector('dialog[open]'))return false;const tag=document.activeElement?.tagName;return !['INPUT','TEXTAREA','SELECT'].includes(tag)}
async function refreshLiveAdmin(){if(!canAutoRefresh()||refreshBusy)return;refreshBusy=true;try{const [dashboard,inquiries,reviews]=await Promise.all([api('/api/admin/dashboard'),api('/api/admin/inquiries'),api('/api/admin/reviews')]);store.dashboard=dashboard;store.inquiries=inquiries;store.reviews=reviews;renderDashboard();renderInquiries();renderReviews();$('#apiState').innerHTML=''}catch(err){console.warn('Admin live refresh failed:',err.message)}finally{refreshBusy=false}}
async function refreshCatalogAdmin(){if(!canAutoRefresh()||refreshBusy||activeView==='settings'||activeView==='password')return;refreshBusy=true;try{const [settings,categories,brands,products,packages]=await Promise.all([api('/api/admin/settings'),api('/api/admin/categories'),api('/api/admin/brands'),api('/api/admin/products'),api('/api/admin/packages')]);store.settings=settings;store.categories=categories;store.brands=brands;store.products=products;store.packages=packages;$('#sideName').textContent=settings.business_name||'POS Philippines';renderSettings();renderCategories();renderBrands();renderProducts();renderPackages();renderDashboard();$('#apiState').innerHTML=''}catch(err){console.warn('Admin catalog refresh failed:',err.message)}finally{refreshBusy=false}}
function startAutoRefresh(){stopAutoRefresh();fastRefreshTimer=setInterval(refreshLiveAdmin,ADMIN_FAST_REFRESH_MS);fullRefreshTimer=setInterval(refreshCatalogAdmin,ADMIN_FULL_REFRESH_MS)}
document.addEventListener('visibilitychange',()=>{const state=$('#autoRefreshState');if(state)state.textContent=document.visibilityState==='visible'?'Auto-refresh: ON':'Auto-refresh: PAUSED';if(document.visibilityState==='visible'){refreshLiveAdmin();refreshCatalogAdmin()}});
function renderAll(){renderDashboard();renderSettings();renderCategories();renderBrands();renderProducts();renderPackages();renderInquiries();renderReviews();renderPassword()}
function openView(v){activeView=v;$$('.section-view').forEach(x=>x.classList.remove('active'));$('#view-'+v)?.classList.add('active');$$('.side-nav [data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$('#pageTitle').textContent=({dashboard:'Dashboard',settings:'Website Settings',categories:'Categories',brands:'Brands',products:'Products',packages:'Packages',inquiries:'Customer Inquiries',reviews:'Customer Reviews',password:'Change Password'})[v]||'Dashboard'}
function renderDashboard(){const d=store.dashboard;$('#view-dashboard').innerHTML=`<div class="dashboard-cards"><div class="stat-card panel"><small>Products</small><strong>${d.products||0}</strong></div><div class="stat-card panel"><small>Active Inquiries</small><strong>${d.active_inquiries||0}</strong></div><div class="stat-card panel"><small>Completed</small><strong>${d.completed_inquiries||0}</strong></div><div class="stat-card panel"><small>Pending Reviews</small><strong>${d.pending_reviews||0}</strong></div></div><div class="grid-2"><div class="panel"><div class="panel-head"><h2>Recent Inquiries</h2><button class="btn btn-outline btn-sm" data-open="inquiries">View All</button></div><div class="panel-body">${store.inquiries.slice(0,5).map(q=>`<div style="padding:10px 0;border-bottom:1px solid var(--line)"><b>${esc(q.tracking)}</b><div class="small muted">${esc(q.name)} • ${esc(q.status)}</div></div>`).join('')||'<div class="empty">No inquiries yet.</div>'}</div></div><div class="panel"><div class="panel-head"><h2>Pending Reviews</h2><button class="btn btn-outline btn-sm" data-open="reviews">Manage</button></div><div class="panel-body">${store.reviews.filter(x=>x.status==='pending').slice(0,5).map(r=>`<div style="padding:10px 0;border-bottom:1px solid var(--line)"><b>${esc(r.name)}</b><div class="small muted">${'★'.repeat(r.rating)} • ${esc(r.message).slice(0,65)}</div></div>`).join('')||'<div class="empty">No pending reviews.</div>'}</div></div></div>`;$$('#view-dashboard [data-open]').forEach(b=>b.onclick=()=>openView(b.dataset.open))}
function renderSettings(){const s=store.settings;$('#view-settings').innerHTML=`<div class="panel"><div class="panel-head"><h2>Website Settings</h2></div><div class="panel-body"><form id="settingsForm" class="form-grid"><div><label>Business Name</label><input name="business_name" class="control" value="${esc(s.business_name||'')}" required></div><div><label>Tagline</label><input name="tagline" class="control" value="${esc(s.tagline||'')}"></div><div class="full"><label>Hero Headline</label><input name="hero_title" class="control" value="${esc(s.hero_title||'')}"></div><div class="full"><label>Hero Description</label><textarea name="hero_subtitle" class="control">${esc(s.hero_subtitle||'')}</textarea></div><div><label>Email</label><input name="email" type="email" class="control" value="${esc(s.email||'')}"></div><div><label>Contact Number</label><input name="phone" class="control" value="${esc(s.phone||'')}"></div><div><label>Facebook Page Name</label><input name="facebook_name" class="control" value="${esc(s.facebook_name||'')}"></div><div><label>Facebook Page Link</label><input name="facebook_url" type="url" class="control" value="${esc(s.facebook_url||'')}"></div><div class="full"><label>Address / Service Area</label><input name="address" class="control" value="${esc(s.address||'')}"></div><div><label>Logo URL</label><input name="logo_url" class="control" value="${esc(s.logo_url||'')}"></div><div><label>Upload Logo</label><input id="settingsLogoUpload" type="file" accept="image/*" class="control"></div><div><label>Hero Image URL</label><input name="hero_image_url" class="control" value="${esc(s.hero_image_url||'')}"></div><div><label>Upload Hero Image</label><input id="settingsHeroUpload" type="file" accept="image/*" class="control"></div><div class="full"><button class="btn btn-blue">Save Website Settings</button></div></form></div></div>`;$('#settingsForm').onsubmit=saveSettings;$('#settingsLogoUpload').onchange=e=>uploadInto(e,$('#settingsForm').logo_url);$('#settingsHeroUpload').onchange=e=>uploadInto(e,$('#settingsForm').hero_image_url)}
async function saveSettings(e){e.preventDefault();try{store.settings=await api('/api/admin/settings',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});renderSettings();$('#sideName').textContent=store.settings.business_name;notify('Website settings saved.','success')}catch(err){notify(err.message,'error')}}
function renderCategories(){const rows=store.categories.map(c=>`<tr><td>${imageCell(c.image_url,c.name)}</td><td><b>${esc(c.name)}</b></td><td>${c.display_order}</td><td><span class="status">${c.is_active?'Active':'Inactive'}</span></td><td><div class="actions"><button class="btn btn-light btn-sm" data-edit-category="${c.id}">Edit</button><button class="btn btn-danger btn-sm" data-delete-category="${c.id}">Delete</button></div></td></tr>`).join('');$('#view-categories').innerHTML=tablePanel('Categories','Add Category','category','<th>Image</th><th>Name</th><th>Order</th><th>Status</th><th>Actions</th>',rows);wireEntityButtons('category')}
function renderBrands(){const rows=store.brands.map(b=>`<tr><td>${imageCell(b.logo_url,b.name)}</td><td><b>${esc(b.name)}</b></td><td>${esc(store.categories.find(c=>c.id===b.category_id)?.name||'—')}</td><td>${b.display_order}</td><td><span class="status">${b.is_active?'Active':'Inactive'}</span></td><td><div class="actions"><button class="btn btn-light btn-sm" data-edit-brand="${b.id}">Edit</button><button class="btn btn-danger btn-sm" data-delete-brand="${b.id}">Delete</button></div></td></tr>`).join('');$('#view-brands').innerHTML=tablePanel('Brands','Add Brand','brand','<th>Logo</th><th>Name</th><th>Category</th><th>Order</th><th>Status</th><th>Actions</th>',rows);wireEntityButtons('brand')}
function renderProducts(){const rows=store.products.map(p=>`<tr><td>${imageCell(p.image_url,p.name)}</td><td><b>${esc(p.name)}</b><div class="small muted">${esc(p.short_description||'')}</div></td><td>${esc(store.categories.find(c=>c.id===p.category_id)?.name||'—')}<div class="small muted">${esc(store.brands.find(b=>b.id===p.brand_id)?.name||'—')}</div></td><td>${peso(p.price)}</td><td><span class="status">${p.is_available?'Visible':'Hidden'}</span></td><td><div class="actions"><button class="btn btn-light btn-sm" data-edit-product="${p.id}">Edit</button><button class="btn btn-danger btn-sm" data-delete-product="${p.id}">Delete</button></div></td></tr>`).join('');$('#view-products').innerHTML=tablePanel('Products','Add Product','product','<th>Image</th><th>Product</th><th>Category / Brand</th><th>Price</th><th>Status</th><th>Actions</th>',rows);wireEntityButtons('product')}
function renderPackages(){const rows=store.packages.map(p=>`<tr><td>${imageCell(p.image_url,p.name)}</td><td><b>${esc(p.name)}</b><div class="small muted">${esc(p.subtitle||'')}</div></td><td>${(p.inclusions||[]).length} item(s)</td><td>${peso(p.price)}</td><td><span class="status">${p.is_active?'Active':'Inactive'}</span></td><td><div class="actions"><button class="btn btn-light btn-sm" data-edit-package="${p.id}">Edit</button><button class="btn btn-danger btn-sm" data-delete-package="${p.id}">Delete</button></div></td></tr>`).join('');$('#view-packages').innerHTML=tablePanel('Packages','Add Package','package','<th>Image</th><th>Name</th><th>Inclusions</th><th>Price</th><th>Status</th><th>Actions</th>',rows);wireEntityButtons('package')}
function tablePanel(title,button,type,heads,rows){return `<div class="panel"><div class="panel-head"><h2>${title}</h2><button class="btn btn-blue btn-sm" data-add-${type}>+ ${button}</button></div><div class="panel-body table-wrap"><table class="data-table"><thead><tr>${heads}</tr></thead><tbody>${rows||`<tr><td colspan="8"><div class="empty">No records yet.</div></td></tr>`}</tbody></table></div></div>`}
function wireEntityButtons(type){const root=$('#view-'+plural(type));if(!root)return;const addButton=root.querySelector(`[data-add-${type}]`);if(addButton)addButton.onclick=()=>entityForm(type);root.querySelectorAll(`[data-edit-${type}]`).forEach(b=>b.onclick=()=>entityForm(type,b.dataset[`edit${type[0].toUpperCase()+type.slice(1)}`]||b.getAttribute(`data-edit-${type}`)));root.querySelectorAll(`[data-delete-${type}]`).forEach(b=>b.onclick=()=>deleteEntity(type,b.getAttribute(`data-delete-${type}`)))}
function categoryOptions(selected=''){return '<option value="">Select category</option>'+store.categories.map(c=>`<option value="${c.id}" ${c.id===selected?'selected':''}>${esc(c.name)}</option>`).join('')}
function brandOptions(categoryId,selected=''){return '<option value="">Select brand</option>'+store.brands.filter(b=>!categoryId||b.category_id===categoryId).map(b=>`<option value="${b.id}" ${b.id===selected?'selected':''}>${esc(b.name)}</option>`).join('')}
function entityForm(type,id=''){const list=store[plural(type)]||[], obj=list.find(x=>x.id===id)||{};$('#entityDialogTitle').textContent=(id?'Edit ':'Add ')+type[0].toUpperCase()+type.slice(1);let fields='';if(type==='category')fields=`<div><label>Category Name</label><input name="name" class="control" value="${esc(obj.name||'')}" required></div><div><label>Display Order</label><input name="display_order" type="number" class="control" value="${obj.display_order??1}"></div><div class="full"><label>Image URL</label><input name="image_url" class="control" value="${esc(obj.image_url||'')}"></div><div><label>Upload Image</label><input id="entityUpload" type="file" accept="image/*" class="control"></div><div><label class="check-row"><input name="is_active" type="checkbox" ${obj.is_active!==0?'checked':''}> Active</label></div>`;if(type==='brand')fields=`<div><label>Category</label><select name="category_id" class="control" required>${categoryOptions(obj.category_id)}</select></div><div><label>Brand Name</label><input name="name" class="control" value="${esc(obj.name||'')}" required></div><div><label>Display Order</label><input name="display_order" type="number" class="control" value="${obj.display_order??1}"></div><div><label>Logo URL</label><input name="logo_url" class="control" value="${esc(obj.logo_url||'')}"></div><div><label>Upload Logo</label><input id="entityUpload" type="file" accept="image/*" class="control"></div><div><label class="check-row"><input name="is_active" type="checkbox" ${obj.is_active!==0?'checked':''}> Active</label></div>`;if(type==='product')fields=`<div><label>Category</label><select name="category_id" id="productCategory" class="control" required>${categoryOptions(obj.category_id)}</select></div><div><label>Brand</label><select name="brand_id" id="productBrand" class="control">${brandOptions(obj.category_id,obj.brand_id)}</select></div><div class="full"><label>Product Name</label><input name="name" class="control" value="${esc(obj.name||'')}" required></div><div><label>Price (0 = Ask for Quote)</label><input name="price" type="number" min="0" class="control" value="${obj.price??0}"></div><div><label>Image URL</label><input name="image_url" class="control" value="${esc(obj.image_url||'')}"></div><div><label>Upload Product Image</label><input id="entityUpload" type="file" accept="image/*" class="control"></div><div class="full"><label>Short Description</label><input name="short_description" class="control" value="${esc(obj.short_description||'')}"></div><div class="full"><label>Full Description</label><textarea name="description" class="control">${esc(obj.description||'')}</textarea></div><div><label class="check-row"><input name="is_featured" type="checkbox" ${obj.is_featured?'checked':''}> Featured</label></div><div><label class="check-row"><input name="is_available" type="checkbox" ${obj.is_available!==0?'checked':''}> Visible / Available</label></div>`;if(type==='package')fields=`<div class="full"><label>Package Name</label><input name="name" class="control" value="${esc(obj.name||'')}" required></div><div><label>Subtitle</label><input name="subtitle" class="control" value="${esc(obj.subtitle||'')}"></div><div><label>Price</label><input name="price" type="number" min="0" class="control" value="${obj.price??0}"></div><div class="full"><label>Image URL</label><input name="image_url" class="control" value="${esc(obj.image_url||'')}"></div><div><label>Upload Package Image</label><input id="entityUpload" type="file" accept="image/*" class="control"></div><div><label>Display Order</label><input name="display_order" type="number" class="control" value="${obj.display_order??1}"></div><div class="full"><label>Inclusions (one per line)</label><textarea name="inclusions" class="control">${esc((obj.inclusions||[]).join('\n'))}</textarea></div><div><label class="check-row"><input name="is_featured" type="checkbox" ${obj.is_featured?'checked':''}> Most popular</label></div><div><label class="check-row"><input name="is_active" type="checkbox" ${obj.is_active!==0?'checked':''}> Active</label></div>`;$('#entityDialogBody').innerHTML=`<form id="entityForm" class="form-grid">${fields}<div class="full"><button class="btn btn-blue btn-block">Save ${type}</button></div></form>`;openDialog('entityDialog');const form=$('#entityForm');if(type==='product')$('#productCategory').onchange=()=>{$('#productBrand').innerHTML=brandOptions($('#productCategory').value)};const fileInput=$('#entityUpload');if(fileInput){const target=form.elements.image_url||form.elements.logo_url;fileInput.onchange=e=>uploadInto(e,target)}form.onsubmit=e=>saveEntity(e,type,id)}
async function uploadInto(e,target){const file=e.target.files[0];if(!file)return;try{const fd=new FormData();fd.append('file',file);const result=await api('/api/admin/upload',{method:'POST',body:fd});target.value=result.url;notify('Image uploaded.','success')}catch(err){notify(err.message,'error')}}
async function saveEntity(e,type,id){e.preventDefault();const f=e.currentTarget,p=Object.fromEntries(new FormData(f));['is_active','is_featured','is_available'].forEach(k=>{if(f.elements[k])p[k]=f.elements[k].checked});if(type==='package')p.inclusions=String(p.inclusions||'').split('\n').map(x=>x.trim()).filter(Boolean);try{const collection=plural(type);const saved=await api(`/api/admin/${collection}${id?'/'+id:''}`,{method:id?'PUT':'POST',body:JSON.stringify(p)});const list=store[collection],idx=list.findIndex(x=>x.id===saved.id);idx>=0?list[idx]=saved:list.push(saved);closeDialog('entityDialog');renderAll();notify(`${type[0].toUpperCase()+type.slice(1)} saved.`,'success')}catch(err){notify(err.message,'error')}}
async function deleteEntity(type,id){if(!confirm(`Delete this ${type}?`))return;try{const collection=plural(type);await api(`/api/admin/${collection}/${id}`,{method:'DELETE'});store[collection]=store[collection].filter(x=>x.id!==id);renderAll();notify('Item deleted.','success')}catch(err){notify(err.message,'error')}}
function renderInquiries(){const rows=store.inquiries.map(q=>`<tr><td><b>${esc(q.tracking)}</b></td><td>${esc(q.name)}<div class="small muted">${esc(q.email)} • ${esc(q.phone)}</div></td><td>${esc(store.categories.find(c=>c.id===q.category_id)?.name||'—')}<div class="small muted">${esc(store.products.find(p=>p.id===q.product_id)?.name||store.brands.find(b=>b.id===q.brand_id)?.name||'General inquiry')}</div></td><td><span class="status ${slug(q.status)}">${esc(q.status)}</span></td><td>${fmt(q.updated_at)}</td><td><button class="btn btn-blue btn-sm" data-manage-inquiry="${q.id}">Manage</button></td></tr>`).join('');$('#view-inquiries').innerHTML=`<div class="panel"><div class="panel-head"><h2>Customer Inquiries</h2></div><div class="panel-body table-wrap"><table class="data-table"><thead><tr><th>Tracking</th><th>Customer</th><th>Interest</th><th>Status</th><th>Updated</th><th>Actions</th></tr></thead><tbody>${rows||'<tr><td colspan="6"><div class="empty">No inquiries yet.</div></td></tr>'}</tbody></table></div></div>`;$$('[data-manage-inquiry]').forEach(b=>b.onclick=()=>manageInquiry(b.dataset.manageInquiry))}
async function manageInquiry(id){try{const q=await api('/api/admin/inquiries/'+id);const f=$('#inquiryUpdateForm');f.id.value=q.id;f.status.innerHTML=STATUSES.map(s=>`<option ${s===q.status?'selected':''}>${s}</option>`).join('');f.followup_date.value=q.followup_date||'';f.public_note.value=q.public_note||'';f.internal_note.value=q.internal_note||'';$('#inquiryDetails').innerHTML=`<div class="result-box"><b>${esc(q.tracking)} — ${esc(q.name)}</b><p class="small">${esc(q.business||'No business name')} • ${esc(q.email)} • ${esc(q.phone)}</p><p><b>Sales:</b> ${esc(q.sales_representative||'Not specified')}</p><p>${esc(q.message)}</p></div>`;$('#inquiryHistory').innerHTML=(q.history||[]).map(h=>`<div class="timeline-item"><span class="timeline-dot"></span><div><b>${esc(h.status)}</b><small>${esc(h.note||'')} • ${fmt(h.created_at)}</small></div></div>`).join('');openDialog('inquiryDialog');$('#inquiryUpdateForm').onsubmit=saveInquiry}catch(err){notify(err.message,'error')}}
async function saveInquiry(e){e.preventDefault();const p=Object.fromEntries(new FormData(e.currentTarget));const id=p.id;delete p.id;try{const q=await api('/api/admin/inquiries/'+id,{method:'PUT',body:JSON.stringify(p)});const idx=store.inquiries.findIndex(x=>x.id===id);if(idx>=0)store.inquiries[idx]=q;closeDialog('inquiryDialog');renderAll();notify('Inquiry updated.','success')}catch(err){notify(err.message,'error')}}
function renderReviews(){const rows=store.reviews.map(r=>`<tr><td><b>${esc(r.name)}</b><div class="small muted">${esc(r.tracking)}</div></td><td><span class="stars">${'★'.repeat(r.rating)}${'☆'.repeat(5-r.rating)}</span></td><td>${r.image_url?`<img src="${esc(r.image_url)}" style="width:54px;height:45px;object-fit:cover;float:left;margin-right:8px">`:''}${esc(r.message)}</td><td><span class="status ${slug(r.status)}">${esc(r.status)}</span></td><td><div class="actions">${r.status!=='approved'?`<button class="btn btn-success btn-sm" data-review-status="approved" data-id="${r.id}">Approve</button>`:''}${r.status!=='rejected'?`<button class="btn btn-outline btn-sm" data-review-status="rejected" data-id="${r.id}">Reject</button>`:''}<button class="btn btn-danger btn-sm" data-delete-review="${r.id}">Delete</button></div></td></tr>`).join('');$('#view-reviews').innerHTML=`<div class="panel"><div class="panel-head"><h2>Customer Reviews</h2></div><div class="panel-body table-wrap"><table class="data-table"><thead><tr><th>Customer</th><th>Rating</th><th>Review</th><th>Status</th><th>Actions</th></tr></thead><tbody>${rows||'<tr><td colspan="5"><div class="empty">No reviews yet.</div></td></tr>'}</tbody></table></div></div>`;$$('[data-review-status]').forEach(b=>b.onclick=()=>reviewStatus(b.dataset.id,b.dataset.reviewStatus));$$('[data-delete-review]').forEach(b=>b.onclick=()=>deleteReview(b.dataset.deleteReview))}
async function reviewStatus(id,status){try{const r=await api(`/api/admin/reviews/${id}/status`,{method:'PUT',body:JSON.stringify({status})});const idx=store.reviews.findIndex(x=>x.id===id);if(idx>=0)store.reviews[idx]=r;renderAll();notify('Review updated.','success')}catch(err){notify(err.message,'error')}}
async function deleteReview(id){if(!confirm('Delete this review?'))return;try{await api('/api/admin/reviews/'+id,{method:'DELETE'});store.reviews=store.reviews.filter(x=>x.id!==id);renderAll();notify('Review deleted.','success')}catch(err){notify(err.message,'error')}}
function renderPassword(){$('#view-password').innerHTML=`<div class="panel" style="max-width:650px"><div class="panel-head"><h2>Change Admin Password</h2></div><div class="panel-body"><form id="changePasswordForm" class="form-grid"><div class="full"><label>Current Password</label><input name="current_password" type="password" class="control" required></div><div><label>New Password</label><input name="new_password" type="password" minlength="8" class="control" required></div><div><label>Confirm New Password</label><input name="confirm_password" type="password" minlength="8" class="control" required></div><div class="full"><button class="btn btn-blue">Change Password</button></div></form></div></div>`;$('#changePasswordForm').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget;if(f.new_password.value!==f.confirm_password.value){notify('New passwords do not match.','error');return}try{await api('/api/auth/change-password',{method:'POST',body:JSON.stringify({current_password:f.current_password.value,new_password:f.new_password.value})});notify('Password changed. Please log in again.','success');logout()}catch(err){notify(err.message,'error')}}}
if(token)startAdmin();else loginView();
})();
